server:
  port: ${CUSTOM.SERVER.PORT:9010}

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/isaneg?useSSL=false&serverTimezone=America/Lima&allowPublicKeyRetrieval=true
    username: robin
    password: robin123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true